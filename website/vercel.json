{
  "buildCommand": "npm run build",
  "outputDirectory": ".next",
  "env": {
    "NEXTAUTH_URL": {
      "description": "Full URL of the deployed site",
      "required": true
    },
    "NEXTAUTH_SECRET": {
      "description": "Session encryption secret. Generate with: openssl rand -base64 32",
      "required": true
    },
    "GITHUB_ID": {
      "description": "GitHub OAuth App Client ID",
      "required": true
    },
    "GITHUB_SECRET": {
      "description": "GitHub OAuth App Client Secret",
      "required": true
    },
    "AWS_REGION": {
      "description": "AWS region for S3 operations",
      "default": "us-east-1"
    },
    "ARTIFACTS_BUCKET": {
      "description": "S3 bucket containing task artifacts (legacy, kept for compatibility)",
      "required": false
    },
    "CREDITS_BUCKET": {
      "description": "S3 bucket containing credit balance data",
      "required": true
    },
    "GITHUB_APP_SLUG": {
      "description": "GitHub App slug for filtering installations",
      "default": "cenetex-coding-agent"
    }
  },
  "redirects": [
    {
      "source": "/dashboard/:path*",
      "destination": "/dashboard/:path*",
      "permanent": false
    },
    {
      "source": "/pricing",
      "destination": "/#pricing",
      "permanent": false
    }
  ]
}
