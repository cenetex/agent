export const metadata = {
  title: "Documentation - Agent",
  description: "Learn how to use the Agent GitHub App",
};

export default function DocsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
