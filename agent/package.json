{
  "name": "@ratimics/github-agent",
  "version": "1.0.0",
  "private": true,
  "description": "Fargate container that runs Codex on GitHub issues/PRs",
  "scripts": {
    "build": "echo 'Agent container - no build step required for TypeScript'"
  }
}
